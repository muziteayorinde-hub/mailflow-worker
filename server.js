import express from "express"; import { ImapFlow } from "imapflow"; import nodemailer from "nodemailer"; import { simpleParser } from "mailparser"; import { createClient } from "@supabase/supabase-js"; const app = express(); app.use(express.json({ limit: "25mb" })); const TOKEN = process.env.MAIL_WORKER_TOKEN; const supabase = createClient( process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY ); app.use((req, res, next) => { if (req.path === "/health") return next(); const auth = req.headers.authorization || ""; if (auth !== `Bearer ${TOKEN}`) { return res.status(401).json({ error: "Unauthorized" }); } next(); }); app.get("/health", (_, res) => { res.json({ ok: true }); }); app.post("/mail/test", async (req, res) => { const { username, password, imap, smtp } = req.body; try { const client = new ImapFlow({ host: imap.host, port: imap.port, secure: imap.secure, auth: { user: username, pass: password }, logger: false }); await client.connect(); await client.logout(); const transporter = nodemailer.createTransport({ host: smtp.host, port: smtp.port, secure: smtp.secure, auth: { user: username, pass: password } }); await transporter.verify(); res.json({ success: true }); } catch (e) { res.status(400).json({ error: e.message }); } }); app.post("/mail/sync", async (req, res) => { const { account_id, username, password, imap, folder = "INBOX", last_uid = 0 } = req.body; const client = new ImapFlow({ host: imap.host, port: imap.port, secure: imap.secure, auth: { user: username, pass: password }, logger: false }); try { await client.connect(); const lock = await client.getMailboxLock(folder); let lastUid = last_uid || 0; try { for await (const msg of client.fetch( { uid: `${(last_uid || 0) + 1}:*` }, { uid: true, envelope: true, source: true, flags: true } )) { const parsed = await simpleParser(msg.source); await supabase.from("emails").insert({ account_id, uid: msg.uid, message_id: parsed.messageId, from_address: parsed.from?.value?.[0]?.address || "", from_name: parsed.from?.value?.[0]?.name, to_addresses: parsed.to?.value || [], subject: parsed.subject, body_text: parsed.text, body_html: parsed.html || null, preview: (parsed.text || "").slice(0, 200), folder: "inbox", is_read: msg.flags?.has("\\Seen") || false, has_attachments: (parsed.attachments?.length || 0) > 0, sent_at: parsed.date, flags: [...(msg.flags || [])] }); if (msg.uid > lastUid) { lastUid = msg.uid; } } } finally { lock.release(); } await client.logout(); res.json({ success: true, last_uid: lastUid }); } catch (e) { try { await client.logout(); } catch {} res.status(400).json({ error: e.message }); } }); app.post("/mail/send", async (req, res) => { const { username, password, smtp, from, to, cc, subject, text, html, attachments } = req.body; try { const transporter = nodemailer.createTransport({ host: smtp.host, port: smtp.port, secure: smtp.secure, auth: { user: username, pass: password } }); const info = await transporter.sendMail({ from, to, cc, subject, text, html, attachments: (attachments || []).map(a => ({ filename: a.filename, content: a.content, encoding: "base64", contentType: a.content_type })) }); res.json({ success: true, message_id: info.messageId }); } catch (e) { res.status(400).json({ error: e.message }); } }); app.listen(process.env.PORT || 3000, () => { console.log("worker up"); });
